# Local Site Directories
_site/
.sass-cache/
.jekyll-cache/
.jekyll-metadata

# To generate Gemfile.lock -> use: $ bundle install
# If unknown encoding name CP720 Error occured -> use: $ chcp.com 1252
Gemfile.lock

# Photoshop Documents
*.psd

# Log Files
*.log

**/.DS_Store
ignore/
# Plan/build protocol docs (local only)
plan_*.md
build_*.md

# API keys / secrets — never commit
.env
.env.*
secrets.yml
